# AGENT_TODO — follow-ups

- [ ] Richer terminal output (markdown / `rich`) for ask mode.
- [ ] `tlm ask --stream` using `provider.stream`.
- [ ] Structured edit round-trip for `tlm do` after `e` in gate (re-parse argv from edited preview).
- [ ] Expand model price table (`tlm/telemetry/prices.py`) and provider-specific tokenizers.
- [ ] AUR / deb packaging when ready for release.
